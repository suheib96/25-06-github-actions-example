name: mein erster workflow

on: workflow_dispatch # manuell auslösen

jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        name: A job to say hello
        steps:
            - name: Hello Step
              run: |
                echo "Hello World"
                date